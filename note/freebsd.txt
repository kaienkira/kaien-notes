===============================================================================
*. 添加用户
    adduser
    pw useradd <user> -m -G <group_list>
*. 把用户添加到组
    pw groupmod <group> -m <newmembers>

===============================================================================
*. update system
    freebsd-update fetch
    freebsd-update install
===============================================================================
*. packages
    vi /usr/local/etc/pkg.conf
        PKG_ENV {
            all_proxy = "socks5://<ip>:<port>"
        }

    pkg bootstrap
    pkg info
    pkg install htop

===============================================================================
*. dns
local-unbound-setup 8.8.8.8

===============================================================================
*. ssh
    vi /etc/ssh/sshd_config
        PermitRootLogin yes
        PasswordAuthentication yes
    service sshd restart
===============================================================================
*. 列出PCI设备
    pciconf -l
*. 列出USB设备
    usbconfig
===============================================================================
