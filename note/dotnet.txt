export DOTNET_CLI_TELEMETRY_OPTOUT=1
export DOTNET_CLI_UI_LANGUAGE=en
export DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1

export DOTNET_TieredCompilation=1       # System.Runtime.TieredCompilation
export DOTNET_TC_QuickJit=1             # System.Runtime.TieredCompilation.QuickJit
export DOTNET_TieredPGO=1

export DOTNET_gcServer=1                # System.GC.Server
export DOTNET_gcConcurrent=1            # System.GC.Concurrent
export DOTNET_GCLOHThreshold=0x1D4C0    # System.GC.LOHThreshold  (120000)
export DOTNET_GCHeapHardLimit=0xC800000 # System.GC.HeapHardLimit (209715200)
export DOTNET_GCHeapHardLimitSOH        # System.GC.HeapHardLimitSOH (Small Object Heap)
export DOTNET_GCHeapHardLimitLOH        # System.GC.HeapHardLimitLOH (Large Object Heap)
export DOTNET_GCHeapHardLimitPOH        # System.GC.HeapHardLimitPOH (Pinned Object Heap)
export DOTNET_GCHeapHardLimitSOHPercent # System.GC.HeapHardLimitSOHPercent
export DOTNET_GCHeapHardLimitLOHPercent # System.GC.HeapHardLimitLOHPercent
export DOTNET_GCHeapHardLimitPOHPercent # System.GC.HeapHardLimitPOHPercent
export DOTNET_GCLargePages=1

export DOTNET_GCRegionRange             # System.GC.RegionRange
export DOTNET_GCRegionSize              # System.GC.RegionSize
export DOTNET_GCHighMemPercent          # System.GC.HighMemoryPercent
export DOTNET_GCRetainVM                # System.GC.RetainVM
export DOTNET_GCConserveMemory=0~9      # System.GC.ConserveMemory
export DOTNET_GCDynamicAdaptationMode

export DOTNET_GCPath=<standalone_gc_lib_path>
export DOTNET_gcAllowVeryLargeObjects=1
------------------------------------------------------------------------------
dotnet --list-sdks
dotnet --list-runtimes
dotnet workload list
dotnet workload search
dotnet workload install

dotnet new console
------------------------------------------------------------------------------
dotnet workload install wasm-tools
dotnet workload install wasm-experimental

------------------------------------------------------------------------------
dotnet tool install -g dotnet-trace
dotnet-trace ps
dotnet-trace list-profiles
dotnet-trace collect -p <pid> --format Speedscope --duration 00:01:00
dotnet-trace collect --format Speedscope -- <program> <args>
dotnet-trace collect -p <pid> --profile gc-verbose
https://www.speedscope.app/

dotnet tool install -g dotnet-dump
dotnet-dump collect -p <pid>
dotnet-dump analyze <core_file>
> clrstack
> threads
> clrstack -a
> logopen dumpheap-stat.txt
> dumpheap -stat
> logclose

dotnet tool install -g dotnet-sos
dotnet-sos install
lldb
> attach <pid>
> thread select <thread_id>
> frame select <frame>
> clrstack -a
> sos ClrStack
> sos DumpStackObjects
> verifyobj <addr>
> dumpobj <addr>

------------------------------------------------------------------------------
<PropertyGroup>
  <ServerGarbageCollection>true</ServerGarbageCollection>
  <ConcurrentGarbageCollection>false</ConcurrentGarbageCollection>
  <RetainVMGarbageCollection>true</RetainVMGarbageCollection>
  <Nullable>enable</Nullable>
  <PublishTrimmed>true</PublishTrimmed>
  <PublishSingleFile>true</PublishSingleFile>
  <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
</PropertyGroup>

------------------------------------------------------------------------------
GCSettings.LatencyMode = GCLatencyMode.SustainedLowLatency;
