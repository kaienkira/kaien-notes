===============================================================================
-*- #include <cmath> -*-
===============================================================================
std::hypot() (abbr hypotenuse)

===============================================================================
-*- #include <algorithm> -*-
===============================================================================
*. std::count()
   std::count_if()
*. std::unique_copy()
*. std::sort()
    比较函数要求对于调用的两个参数交换位置时不能得到相同的true的结果
*. std::transform()

===============================================================================
-*- #include <any> -*-
===============================================================================
*. std::any
   std::any_cast<T>()
   std::make_any()

===============================================================================
-*- #include <concepts> -*-
===============================================================================
*. std::integral

===============================================================================
-*- #include <numbers> -*-
===============================================================================
x86_64
    float -> 4bytes precision
    double -> 8bytes precision
    long double -> 10bytes precision
arm64
    float -> 4bytes precision
    double -> 8bytes precision
    long double -> 8bytes precision

*. std::e_v<double>
*. std::pi_v<double>

===============================================================================
-*- #include <type_traits> -*-
===============================================================================
*. std::is_integral<T>  ref: std::numeric_limits<T>::is_integer

   std::is_integral_v<T>
-------------------------------------------------------------------------------
*. std::is_reference<T>
   std::is_lvalue_reference<T>
   std::is_rvalue_reference<T>

   std::is_reference_v<T>
   std::is_lvalue_reference_v<T>
   std::is_rvalue_reference_v<T>
-------------------------------------------------------------------------------
*. std::remove_cv<T>
   std::remove_const<T>
   std::remove_volatile<T>

   std::remove_cv_t<T>
   std::remove_const_t<T>
   std::remove_volatile_t<T>
-------------------------------------------------------------------------------
*. std::integral_constant

   std::true_type
   std::false_type
-------------------------------------------------------------------------------
*. std::is_same<T, U>

   std::is_same_v<T, U>
-------------------------------------------------------------------------------
