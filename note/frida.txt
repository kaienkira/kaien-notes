# install
python -m venv ~/local/frida-py
source ~/local/frida-py/bin/activate
pip install frida frida-tools
pip install --upgrade frida frida-tools

# enable ptrace
cat /proc/sys/kernel/yama/ptrace_scope
    0 - classic ptrace permissions
    1 - restricted ptrace
        its descendants
        prctl(PR_SET_PTRACER, debugger, ...)
    2 - admin-only attach
        only processes with CAP_SYS_PTRACE may use ptrace
    3 - no attach
        Once set, this sysctl value cannot be changed
sudo sysctl kernel.yama.ptrace_scope=0

# android
https://github.com/frida/frida/releases/
https://github.com/frida/frida/releases/download/<version>/frida-server-<version>-android-arm64.xz

# objection
python -m venv ~/local/objection-py
pip install frida==16.7.19 objection
objection patchapk -s <source.apk> -a arm64-v8a -V 16.7.19
    > android hooking list activities
    > android intent launch_activity <activity>
    > memory list modules
